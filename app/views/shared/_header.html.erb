<div class="navbar navbar-default navbar-fixed-top no-print">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Major Project</a>
      <button class="navbar-toggle" data-target=".navbar-collapse" data-toggle="collapse" type="button">
         <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="navbar-collapse collapse pull-right">
      <ul class="nav navbar-nav">
        <% if current_user %>
         <% if can_administer? %>
          <li>
           <%= link_to "New Survey", rapidfire.new_question_group_path %>
          </li>
         <% end %>
          <li>
            <%= link_to "Logout", main_app.session_path, method: :delete %>
          </li>
        <% else %>
          <li>
            <%= link_to "Login", main_app.new_session_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
